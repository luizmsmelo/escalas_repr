# O app roda no Cloudflare Pages (ver README). Este arquivo fica para o deploy
# no Netlify continuar possivel. Os cabecalhos estao em public/_headers, que as
# duas plataformas leem.
[build]
  publish = "public"
  functions = "netlify/functions"
  command = "echo 'sem build - front-end estatico'"

[functions]
  node_bundler = "esbuild"
